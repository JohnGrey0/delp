<UserControl x:Class="Delp.App.Tools.DataFormat.JsonTreeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>

        <!-- Local styles: nothing in Theme.xaml styles TreeView, so this view supplies its own,
             using only theme Brush.* resources — never a hardcoded color. -->

        <Style x:Key="Json.TreeViewItemStyle" TargetType="TreeViewItem">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{StaticResource Brush.Fg0}" />
            <Setter Property="Padding" Value="4,3" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}" />
            <Setter Property="ItemsPanel">
                <Setter.Value>
                    <ItemsPanelTemplate>
                        <VirtualizingStackPanel IsItemsHost="True" />
                    </ItemsPanelTemplate>
                </Setter.Value>
            </Setter>
            <EventSetter Event="Expanded" Handler="TreeViewItem_Expanded" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TreeViewItem">
                        <StackPanel>
                            <Border x:Name="Bd" CornerRadius="5">
                                <Grid Margin="{TemplateBinding Padding}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="16" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <ToggleButton x:Name="Expander"
                                                  Grid.Column="0"
                                                  Width="16"
                                                  Height="16"
                                                  Focusable="False"
                                                  ClickMode="Press"
                                                  IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}">
                                        <ToggleButton.Template>
                                            <ControlTemplate TargetType="ToggleButton">
                                                <Border Background="Transparent">
                                                    <Path x:Name="Arrow"
                                                          Data="M1,1 L7,5 L1,9"
                                                          Stroke="{StaticResource Brush.Fg1}"
                                                          StrokeThickness="1.6"
                                                          StrokeStartLineCap="Round"
                                                          StrokeEndLineCap="Round"
                                                          StrokeLineJoin="Round"
                                                          HorizontalAlignment="Center"
                                                          VerticalAlignment="Center"
                                                          RenderTransformOrigin="0.5,0.5">
                                                        <Path.RenderTransform>
                                                            <RotateTransform Angle="0" />
                                                        </Path.RenderTransform>
                                                    </Path>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsChecked" Value="True">
                                                        <Setter TargetName="Arrow" Property="RenderTransform">
                                                            <Setter.Value>
                                                                <RotateTransform Angle="90" />
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </ToggleButton.Template>
                                    </ToggleButton>
                                    <ContentPresenter Grid.Column="1"
                                                       ContentSource="Header"
                                                       VerticalAlignment="Center" />
                                </Grid>
                            </Border>
                            <ItemsPresenter x:Name="ItemsHost" Margin="16,0,0,0" />
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="HasItems" Value="False">
                                <Setter TargetName="Expander" Property="Visibility" Value="Hidden" />
                            </Trigger>
                            <Trigger Property="IsExpanded" Value="False">
                                <Setter TargetName="ItemsHost" Property="Visibility" Value="Collapsed" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource Brush.AccentSoft}" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#14FFFFFF" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="Json.TreeViewStyle" TargetType="TreeView">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Foreground" Value="{StaticResource Brush.Fg0}" />
            <Setter Property="Padding" Value="2" />
            <Setter Property="ScrollViewer.CanContentScroll" Value="True" />
            <Setter Property="VirtualizingPanel.IsVirtualizing" Value="True" />
            <Setter Property="VirtualizingPanel.VirtualizationMode" Value="Recycling" />
            <Setter Property="VirtualizingPanel.ScrollUnit" Value="Pixel" />
            <Setter Property="ItemsPanel">
                <Setter.Value>
                    <ItemsPanelTemplate>
                        <VirtualizingStackPanel IsItemsHost="True" />
                    </ItemsPanelTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TreeView">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <!-- Internal scroll surface owned by this control's own template — not
                                 an outer ScrollViewer wrapped around the TreeView from outside,
                                 so it doesn't trip the wheel-trap / virtualization-breaking lint. -->
                            <ScrollViewer Padding="{TemplateBinding Padding}"
                                          CanContentScroll="True"
                                          Focusable="False">
                                <ItemsPresenter />
                            </ScrollViewer>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <HierarchicalDataTemplate x:Key="Json.NodeTemplate" ItemsSource="{Binding Children}">
            <TextBlock Text="{Binding Header}"
                       FontFamily="{StaticResource Font.Mono}"
                       FontSize="12.5"
                       TextTrimming="CharacterEllipsis" />
        </HierarchicalDataTemplate>

    </UserControl.Resources>

    <DockPanel Margin="16">
        <TabControl>

            <TabItem Header="INPUT">
                <DockPanel Margin="0,10,0,0">
                    <WrapPanel DockPanel.Dock="Top" Margin="0,0,0,10">
                        <Button x:Name="LoadBtn"
                                Style="{StaticResource Button.Primary}"
                                Content="Load"
                                Click="Load_Click"
                                Margin="0,0,10,8" />
                        <TextBlock VerticalAlignment="Center"
                                   Style="{StaticResource Text.Sub}"
                                   Text="Auto-loads up to 2 MB; use Load for larger documents."
                                   Margin="0,0,0,8" />
                    </WrapPanel>

                    <TextBlock x:Name="InputErrorText"
                               DockPanel.Dock="Bottom"
                               Margin="0,8,0,0"
                               Style="{StaticResource Text.Error}"
                               Visibility="Collapsed" />

                    <Border x:Name="InputHost" Style="{StaticResource Card.Editor}" />
                </DockPanel>
            </TabItem>

            <TabItem Header="TREE">
                <DockPanel Margin="0,10,0,0">
                    <StackPanel DockPanel.Dock="Top" Margin="0,0,0,10">
                        <TextBox x:Name="SearchBox"
                                 Style="{StaticResource TextBox.Search}"
                                 Tag="Search keys and values…"
                                 TextChanged="SearchBox_TextChanged" />
                        <TextBlock x:Name="SearchStatusText"
                                   Margin="0,6,0,0"
                                   Style="{StaticResource Text.Sub}" />
                    </StackPanel>

                    <StackPanel DockPanel.Dock="Bottom" Margin="0,10,0,0">
                        <TextBlock x:Name="NodesShownText"
                                   Margin="0,0,0,8"
                                   Style="{StaticResource Text.Sub}" />

                        <DockPanel Margin="0,0,0,6">
                            <Button x:Name="CopyPathBtn" DockPanel.Dock="Right" Content="Copy" Click="CopyPath_Click" />
                            <TextBlock DockPanel.Dock="Left"
                                       Width="60"
                                       VerticalAlignment="Center"
                                       Style="{StaticResource Text.Section}"
                                       Text="PATH" />
                            <TextBox x:Name="PathBox" Margin="8,0" IsReadOnly="True" FontFamily="{StaticResource Font.Mono}" />
                        </DockPanel>
                        <DockPanel Margin="0,0,0,6">
                            <Button x:Name="CopyPointerBtn" DockPanel.Dock="Right" Content="Copy" Click="CopyPointer_Click" />
                            <TextBlock DockPanel.Dock="Left"
                                       Width="60"
                                       VerticalAlignment="Center"
                                       Style="{StaticResource Text.Section}"
                                       Text="POINTER" />
                            <TextBox x:Name="PointerBox" Margin="8,0" IsReadOnly="True" FontFamily="{StaticResource Font.Mono}" />
                        </DockPanel>
                        <DockPanel>
                            <Button x:Name="CopyValueBtn" DockPanel.Dock="Right" Content="Copy" Click="CopyValue_Click" />
                            <TextBlock DockPanel.Dock="Left"
                                       Width="60"
                                       VerticalAlignment="Center"
                                       Style="{StaticResource Text.Section}"
                                       Text="VALUE" />
                            <TextBox x:Name="ValueBox" Margin="8,0" IsReadOnly="True" FontFamily="{StaticResource Font.Mono}" />
                        </DockPanel>
                    </StackPanel>

                    <!-- No outer ScrollViewer around this TreeView — it scrolls itself (see the
                         internal ScrollViewer in Json.TreeViewStyle's ControlTemplate above). -->
                    <TreeView x:Name="Tree"
                              Style="{StaticResource Json.TreeViewStyle}"
                              ItemContainerStyle="{StaticResource Json.TreeViewItemStyle}"
                              ItemTemplate="{StaticResource Json.NodeTemplate}"
                              SelectedItemChanged="Tree_SelectedItemChanged" />
                </DockPanel>
            </TabItem>

        </TabControl>
    </DockPanel>
</UserControl>
