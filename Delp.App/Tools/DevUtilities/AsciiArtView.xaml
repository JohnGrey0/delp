<UserControl x:Class="Delp.App.Tools.DevUtilities.AsciiArtView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DockPanel Margin="16">

        <TextBlock x:Name="ErrorText"
                   DockPanel.Dock="Bottom"
                   Margin="0,8,0,0"
                   Style="{StaticResource Text.Error}"
                   Visibility="Collapsed" />

        <StackPanel DockPanel.Dock="Top" Margin="0,0,0,12">
            <TextBlock Style="{StaticResource Text.Section}" Text="TEXT" Margin="0,0,0,4" />
            <TextBox x:Name="TextInputBox" Text="Hi" TextChanged="Input_Changed" Margin="0,0,0,10" />

            <TextBlock Style="{StaticResource Text.Section}" Text="FONT" Margin="0,0,0,4" />
            <ComboBox x:Name="FontCombo"
                      SelectionChanged="Input_Changed"
                      MinWidth="140"
                      HorizontalAlignment="Left" />
        </StackPanel>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <DockPanel Grid.Row="0" Margin="0,0,0,6">
                <Button x:Name="CopyBtn" DockPanel.Dock="Right" Content="Copy" Click="Copy_Click" />
                <TextBlock VerticalAlignment="Center" Style="{StaticResource Text.Section}" Text="OUTPUT" />
            </DockPanel>

            <Border Grid.Row="1" Style="{StaticResource Card.Editor}">
                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                    <TextBlock x:Name="OutputText"
                               FontFamily="{StaticResource Font.Mono}"
                               FontSize="13"
                               Foreground="{StaticResource Brush.Fg0}"
                               TextWrapping="NoWrap" />
                </ScrollViewer>
            </Border>
        </Grid>
    </DockPanel>
</UserControl>
